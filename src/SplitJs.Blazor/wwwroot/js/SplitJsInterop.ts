///<reference path="../node_modules/split.js/index.d.ts" />

class SplitJsInterOp {
    public SplitElements(elements: HTMLElement[], direction: "horizontal" | "vertical", gutterSize: number, sizes: number[], minSizes: number[]) {
        Split(elements, {
        })
    }
}